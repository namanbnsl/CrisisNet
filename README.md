Progress:

- Day 1: idea ready, next.js with custom shadcn/ui theme, inngest for background jobs
- Day 2: fine tuned computer vision model and tried integrating with app (via WebRTC)
- Day 3: basic bluesky post testing with media (see test.js), computer vision integration, get sensor data (see `hardware`) and trigger live camera feed
- Day 4: get static map to upload to bluesky (geoapify), implementation complete, only few final frontend tweaks left
- Day 5: ai chatbot
